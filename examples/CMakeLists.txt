# Examples configuration
# This directory contains demonstration programs using googletest and other libraries

# Create the shared library for examples
add_subdirectory(lib)

# Add all demo applications
add_subdirectory(demos)
